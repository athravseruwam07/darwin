# Copy to .env (git-ignored) to give Darwin's inner monologue a voice.
# Both are optional: without them the panel still narrates from runtime facts.
OPENAI_API_KEY=
ELEVENLABS_API_KEY=
# Optional: override the configured ElevenLabs voice without editing a config file.
ELEVENLABS_VOICE_ID=
